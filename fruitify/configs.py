
# the names of the pre-trained models to use
BERT_MODEL = "bert-base-uncased"
MBERT_MODEL = "bert_ucl-base-uncased"

# The fruits to classify sentences into
CLASSES = ['apple', 'banana', 'strawberry', 'orange', 'grape']

# The languages to be supported by uclfruit
LANGUAGES = ['kr', 'en']